# Publication File Map

The repository directory `supplementary_data_files/` contains the two journal-uploaded CSV files and `Supplementary_Dataset_README.txt`, which is the repository-readable counterpart of the README PDF supplied separately to the journal. The TXT counterpart is not an additional journal-uploaded supplementary file. Repository-only detailed outputs are under `results/`.
